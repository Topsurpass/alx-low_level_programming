#include <stdio.h>
/**
 * main - The entry point for all other functions and variables
 * Return: Always 0 (means success)
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
		printf("%d", i);
	}
	putchar('\n');
	return (0);
}
