/**
 * mul - Multiply two numbers
 * @a: First argument of type integer
 * @b: Second argument of type integer
 * Return: product of the 2 arguments
 */
int mul(int a, int b)
{
	int product = (a * b);

	return (product);
}
